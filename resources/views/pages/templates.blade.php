
@extends('layouts.app')
@section('content')
    <h1>templates</h1>
    <div class="row">
{{--        @foreach($templates as $template)
            <div class="col-md-3">
                <p>{{$template->title}}</p>
                <p>{{$template->body}}</p>
            </div>
        @endforeach--}}
    </div>

@endsection